# --- Core ---
python-dotenv>=1.0.0
truststore>=0.9.0
pydantic>=2.6.0

# --- Azure SDKs ---
azure-identity>=1.16.0
azure-storage-blob>=12.19.0
azure-search-documents==11.6.0b4
azure-ai-documentintelligence>=1.0.0b3
azure-cosmos>=4.7.0
azure-core>=1.30.0

# --- Chat memory (Redis / Azure Cache for Redis) ---
redis>=5.0.0

# --- LLM ---
openai>=1.30.0

# --- PDF / image extraction ---
PyMuPDF>=1.24.0
Pillow>=10.0.0

# --- API server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sse-starlette>=2.0.0

# --- Auth (Azure AD JWT validation) ---
PyJWT[crypto]>=2.8.0
requests>=2.31.0

# --- Notebooks (dev only) ---
jupyter>=1.0.0
ipykernel>=6.29.0
